# ⚠️ Archived — moved to knpc-market-dashboard

This repo mixed an old Streamlit prototype with a later FastAPI+React
rebuild (`feature/monolith-rebuild`). It has been consolidated and is now
archived.

**Active repo:** https://github.com/BT-Rajan/knpc-market-dashboard

That repo contains the FastAPI + React + MySQL version only, with the
security fixes applied (bcrypt-hashed credentials via env, no plaintext
passwords, restricted CORS). Do not use this repo for new work.
